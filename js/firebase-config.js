// Fill this in with your own Firebase project's web app config to turn on
// cross-device syncing (so you and your partner see the same shared data).
// Without it, the app still works fine — it just stays local to each device.
//
// See README.md for step-by-step setup instructions (it takes about 3 minutes,
// no coding involved).
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
