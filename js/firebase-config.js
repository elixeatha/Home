// Firebase Realtime Database config. The database URL is all this app needs
// to sync data — see README.md for the security rules you still need to set
// in the Firebase console so both of you can actually read/write it.
export const firebaseConfig = {
  databaseURL: "https://home-53bfe-default-rtdb.europe-west1.firebasedatabase.app",
};
